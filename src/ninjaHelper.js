/**
 * Use ninja API or use email reports
 * For API https://app.ninjarmm.com/apidocs/?links.active=core#/queries/getInstalledOSPatches
 * - /v2/queries/os-patch-installs
 * - /v2/queries/os-patches
 * - /v2/organization/{id}/devices
 *
 * Alternatively, can probably request a report in ninja to reports@atlanticdigital.com.au and parse os patching data from that report
 */