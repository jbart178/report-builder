/**
 * Use scheduled reports for backup radar
 * Read csv from reports@atlanticdigital.com.au when it arrives and then use *
 * data to build report.
 * Scheduled repoorts are already set up, would need to change format to not
 * pdf (probably csv) then read csv and generate report format
 *
 * https://us.backupradar.com/app/reporting/scheduled
 */